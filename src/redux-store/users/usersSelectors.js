const getUsers = state => state.users;
const getUser = state => state.user;
const getLoading = state => state.loading;
const getError = state => state.error;

export default { getUsers, getUser, getLoading, getError };
